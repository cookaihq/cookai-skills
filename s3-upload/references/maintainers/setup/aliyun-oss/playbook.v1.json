{
  "schema_version": 1,
  "artifact_type": "s3-upload-provider-setup-playbook",
  "provider": "aliyun-oss",
  "contract_id": "aliyun-oss.setup.console.v1",
  "surface_version": "aliyun-oss-console.synthetic.v1",
  "registry_revision": "aliyun-oss-setup-registry.v1",
  "fixture_kind": "synthetic/docs-derived",
  "network_requests_allowed": false,
  "live_evidence_ids": [],
  "data_plane_evidence_enables_setup": false,
  "remote_evidence": "not-tested",
  "process_only_temporary_credential": {
    "allowed_as_verification_input": true,
    "issued_by_setup": false,
    "persisted_by_setup": false,
    "source_id": "aliyun-oss-sts"
  },
  "credential_issuance": {
    "identity": "least-privilege RAM application user",
    "credential": "long-lived Access Key pair",
    "root_key_allowed": false,
    "session_token": "",
    "expires_at": null,
    "handoff": "plan-and-action-bound one-shot memory sink",
    "unknown_or_local_failure": "manual revoke-and-reissue"
  },
  "scenarios": [
    {
      "id": "new-dedicated-bucket",
      "automatic_boundary": "Bucket is absent, dedicated, exact region/account identity is observed, and all public controls changed by the plan are creation-time controls on that new bucket.",
      "actions": ["create-dedicated-bucket", "apply-new-bucket-public-read"],
      "blocked_when": ["account-level change", "unverified console marker", "missing HTTPS Public Base URL"]
    },
    {
      "id": "existing-exclusive-prefix",
      "automatic_boundary": "Prefix is non-empty, exclusive, empty, non-overlapping, and account/bucket controls already permit a strictly prefix-scoped anonymous GetObject policy.",
      "actions": ["apply-prefix-public-read", "merge-prefix-lifecycle"],
      "blocked_when": ["existing object", "shared or overlapping prefix", "bucket-wide control change", "account-level control change"]
    },
    {
      "id": "bucket-cors",
      "automatic_boundary": "Only a newly created dedicated bucket is eligible because CORS is bucket-scoped.",
      "actions": ["merge-bucket-cors"],
      "blocked_when": ["existing bucket", "overlapping rule", "unrelated rule cannot be preserved exactly"]
    }
  ],
  "merge_rules": {
    "lifecycle": "Read all rules, retain unrelated rows in order, reject any prefix overlap, and append or exactly match one deterministic managed id.",
    "cors": "Read all rules, retain unrelated rows in order, reject origin/method overlap, and append or exactly match one deterministic managed id."
  },
  "endpoint_boundary": {
    "mainland_default_public": "blocked because PublicEndpointForbidden cannot be repaired by Public Base URL",
    "eligible_vpc_internal": "separate data contract and future evidence",
    "bucket_bound_cname": "separate data contract and future evidence"
  },
  "ticket_26_scope": "The authorized OSS credential covers only the bounded data-plane unique prefix. It grants no control-plane authority and produces no setup evidence.",
  "enablement": "Requires separate human-gated console recording, reviewed setup evidence, and an enabling ADR."
}
